import './style.scss';

export const Auth = () => {
  return (
    <div className="auth-screen-container">
      <p>Hello, this is auth screen. (-:</p>
      <button onClick={() => {}}>go home</button>
    </div>
  );
};
